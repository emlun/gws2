[package]
name = "gws2"
version = "0.1.0"
description = "gws is a helper to manage workspaces which contain git repositories."
authors = ["Emil Lundberg <emil@emlun.se>"]

[dependencies]
ansi_term = "0.11"
clap = { version = "2.31", features = ["yaml"] }
git2 = "0.7"

[dev-dependencies]
tempdir = "0.3"
