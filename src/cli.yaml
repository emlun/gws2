name: gws
about: gws is a helper to manage workspaces which contain git repositories.

subcommands:
  - status:
      about: Print status for all repositories in the workspace
      args:
        - only_changes:
            help: Only print out-of-sync repositories and branches
            long: only-changes
